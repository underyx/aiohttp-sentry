dependencies:
  override:
    - pip install tox~=3.1.2 tox-pyenv~=1.1 tox-battery~=0.5.1
    - pyenv local 3.7.0
    - tox --notest
  cache_directories:
    - .tox
test:
  override:
    - tox -e coala -- --non-interactive
    - tox -e pylint
